# Architecture (fixture)

The entry point lives in `src/real.ts` and the function is defined at
`src/real.ts:1`.

A stale reference to a removed file: `src/ghost.ts`.

An out-of-range citation: `src/real.ts:999`.

Prose mention of a doc like README.md should not be flagged.
